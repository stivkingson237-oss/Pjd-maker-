import React,{useEffect,useState}from'react';import{Heart,ShoppingCart,Store,User,X,Mail}from'lucide-react';import{supabase}from'./lib/supabase';import'./marketplace-topnav.css';
export default function MarketplaceTopNav({session,onOpenCart,onOpenSeller,onOpenAccount}){const[shop,setShop]=useState(null);const loadShop=async()=>{if(!session){setShop(null);return}const{data}=await supabase.from('shops').select('*').eq('owner_id',session.user.id).maybeSingle();setShop(data||null)};useEffect(()=>{loadShop();const handler=()=>loadShop();window.addEventListener('pjd-shop-updated',handler);return()=>window.removeEventListener('pjd-shop-updated',handler)},[session?.user?.id]);return <div className="pjd-topnav"><div className="pjd-topnav-brand"><b>PJD</b><span>MAKER</span></div><div className="pjd-topnav-actions"><button className="pjd-action"onClick={()=>onOpenAccount?.('favorites')}><Heart/><span>Favoris</span></button><button className="pjd-action"onClick={onOpenCart}><ShoppingCart/><span>Panier</span></button>{shop?<button className="pjd-action pjd-shop"onClick={()=>onOpenSeller?.(shop)}><Store/><span>Ma boutique</span></button>:<button className="pjd-create-shop"onClick={()=>onOpenSeller?.(null)}><Store/><span>Créer ma boutique</span></button>}<button className="pjd-action"onClick={()=>onOpenAccount?.('account')}><User/><span>{session?'Mon compte':'Se connecter'}</span></button></div></div>}
export function CreateShopFlow({session,onDone,onClose}){const[mode,setMode]=useState(session?'shop':'signup'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[code,setCode]=useState(''),[msg,setMsg]=useState(''),[busy,setBusy]=useState(false),[activeSession,setActiveSession]=useState(session),[form,setForm]=useState({name:'',category:'',description:'',phone:'',city:'',country:'Cameroun'});useEffect(()=>{if(session){setActiveSession(session);setMode('shop')}},[session]);const update=(k,v)=>setForm(f=>({...f,[k]:v}));async function signup(e){e.preventDefault();setBusy(true);const clean=email.trim().toLowerCase();const{error}=await supabase.auth.signUp({email:clean,password,options:{data:{name:clean.split('@')[0]}}});if(error)setMsg(error.message);else{setMode('verify');setMsg('Compte créé. Entrez le code de confirmation à 6 chiffres reçu par e-mail.')}setBusy(false)}async function verify(e){e.preventDefault();setBusy(true);const{data,error}=await supabase.auth.verifyOtp({email:email.trim().toLowerCase(),token:code,type:'signup'});if(error)setMsg(error.message);else{setActiveSession(data.session);setMode('shop');setMsg('Compte confirmé. Complétez les informations de votre boutique.')}setBusy(false)}async function saveShop(e){e.preventDefault();if(!activeSession)return;setBusy(true);const slug=form.name.toLowerCase().trim().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')+'-'+activeSession.user.id.slice(0,6);const{data,error}=await supabase.from('shops').insert({owner_id:activeSession.user.id,shop_name:form.name.trim(),slug,description:form.description.trim(),category:form.category.trim(),phone:form.phone.trim(),city:form.city.trim(),country:form.country.trim(),status:'pending'}).select().single();if(error)setMsg(error.message);else{await supabase.from('users').update({role:'vendeur',shop_id:data.id}).eq('id',activeSession.user.id);window.dispatchEvent(new CustomEvent('pjd-shop-updated',{detail:data}));onDone?.(data)}setBusy(false)}return <div className="pjd-flow-backdrop"><div className="pjd-flow"><button className="pjd-flow-close"onClick={onClose}><X/></button>{mode==='signup'&&<><div className="pjd-flow-title"><Store/><span>Créer ma boutique</span></div><h2>Créez votre compte vendeur</h2><p>Inscrivez-vous pour créer et gérer votre boutique PJD Maker.</p><form onSubmit={signup}><label>E-mail<input type="email"value={email}onChange={e=>setEmail(e.target.value)}required/></label><label>Mot de passe<input type="password"value={password}onChange={e=>setPassword(e.target.value)}minLength={6}required/></label><button className="pjd-primary"disabled={busy}>{busy?'Création...':'Créer mon compte'}</button></form></>}{mode==='verify'&&<><div className="pjd-flow-title"><Mail/><span>Vérification</span></div><h2>Confirmez votre e-mail</h2><p>Entrez le code à 6 chiffres reçu à <b>{email}</b>.</p><form onSubmit={verify}><input className="pjd-code"inputMode="numeric"maxLength={6}pattern="[0-9]{6}"value={code}onChange={e=>setCode(e.target.value.replace(/\D/g,''))}placeholder="000000"required/><button className="pjd-primary"disabled={busy}>{busy?'Vérification...':'Continuer'}</button></form></>}{mode==='shop'&&<><div className="pjd-flow-title"><Store/><span>Ma boutique</span></div><h2>Configurez votre boutique</h2><p>Ces informations sont nécessaires pour commencer à vendre.</p><form onSubmit={saveShop}><label>Nom de la boutique *<input value={form.name}onChange={e=>update('name',e.target.value)}required placeholder="Ex. PJD Tech Store"/></label><label>Activité / catégorie *<input value={form.category}onChange={e=>update('category',e.target.value)}required placeholder="Mode, électronique, services..."/></label><label>Description<textarea value={form.description}onChange={e=>update('description',e.target.value)}rows={3}/></label><div className="pjd-two"><label>Téléphone<input value={form.phone}onChange={e=>update('phone',e.target.value)}/></label><label>Ville<input value={form.city}onChange={e=>update('city',e.target.value)}/></label></div><label>Pays<input value={form.country}onChange={e=>update('country',e.target.value)}required/></label><button className="pjd-primary"disabled={busy}>{busy?'Enregistrement...':'Créer ma boutique et accéder à mon espace vendeur'}</button></form></>}{msg&&<div className="pjd-flow-msg">{msg}</div>}</div></div>}
