import React,{useEffect,useMemo,useState}from'react';import{Search,ShoppingCart,User,Menu,X,ChevronRight,Star,Heart,Download,ShieldCheck,Zap,Store,LogIn,LogOut,Loader2,Mail,ArrowLeft,BarChart3,Package,Wallet,Truck,MessageSquare,Plus,Share2,Copy,CheckCircle,Image as ImageIcon}from'lucide-react';import{supabase}from'./lib/supabase';import'./marketplace.css';import'./payment.css';
const nav=['Accueil','Catalogue','Produits physiques','Produits numériques','Services','Promotions'];const money=v=>Number(v||0)===0?'Gratuit':`${Number(v||0).toLocaleString('fr-FR')} FCFA`;
async function downloadFreeProduct(product){if(!product?.file_url){alert('Le fichier de ce produit gratuit n’est pas encore disponible.');return}try{const response=await fetch(product.file_url);if(!response.ok)throw new Error('Téléchargement impossible');const blob=await response.blob();const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=product.title||'produit-gratuit';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1000)}catch{window.open(product.file_url,'_blank','noopener,noreferrer')}}
function ProductCard({p,onAdd}){const[downloading,setDownloading]=useState(false);const free=Boolean(p.is_free)||Number(p.price||0)===0;async function handleDownload(){setDownloading(true);await downloadFreeProduct(p);setDownloading(false)}return <article className="product-card"><div className="product-art">{p.cover_image?<img src={p.cover_image}alt=""loading="lazy"/>:<Store size={54}/>}<button className="wish"aria-label="Favori"><Heart size={18}/></button></div><div className="product-body"><div className="product-cat">{p.category||'Produit'}</div><h3>{p.title}</h3><div className="rating"><Star size={15}fill="currentColor"/>4.8 <span>Vendeur vérifié</span></div><div className="price"><strong>{money(p.promo_price??p.price)}</strong>{p.promo_price!=null&&Number(p.promo_price)<Number(p.price)&&<del>{money(p.price)}</del>}</div>{free?<button className="add"type="button"onClick={handleDownload}disabled={downloading}><Download size={17}/>{downloading?'Téléchargement...':'Téléchargez gratuitement'}</button>:<button className="add"onClick={()=>onAdd(p)}><ShoppingCart size={17}/>Ajouter au panier</button>}</div></article>}
function AuthModal({mode,setMode,email,setEmail,password,setPassword,code,setCode,onSignIn,onSignUp,onVerify,onClose,loading,message}){return <div className="modal-backdrop"onClick={onClose}><div className="cart-modal auth-modal"onClick={e=>e.stopPropagation()}><div className="modal-head"><div><span className="eyebrow">PJD MARKET</span><h2>{mode==='verify'?'Vérifiez votre e-mail':mode==='signup'?'Créer un compte':'Se connecter'}</h2></div><button onClick={onClose}><X/></button></div>{message&&<div className="account-message">{message}</div>}{mode==='verify'?<form onSubmit={onVerify}><div className="auth-icon"><Mail/></div><p>Entrez le code à 6 chiffres reçu à <b>{email}</b>.</p><label><span>Code de confirmation</span><input inputMode="numeric"maxLength={6}pattern="[0-9]{6}"value={code}onChange={e=>setCode(e.target.value.replace(/\D/g,''))}placeholder="000000"autoFocus/></label><button className="primary auth-submit"disabled={loading}>{loading?'Vérification...':'Vérifier le code'}</button><button type="button"className="secondary auth-link"onClick={()=>setMode('signup')}><ArrowLeft size={16}/>Modifier l’adresse</button></form>:<form onSubmit={mode==='signup'?onSignUp:onSignIn}><label><span>Adresse e-mail</span><input type="email"value={email}onChange={e=>setEmail(e.target.value)}required/></label><label><span>Mot de passe</span><input type="password"value={password}onChange={e=>setPassword(e.target.value)}required minLength={6}/></label>{mode==='signup'&&<p className="auth-help">Un code à 6 chiffres sera envoyé après l’inscription.</p>}<button className="primary auth-submit"disabled={loading}>{loading?'Patientez...':mode==='signup'?'Créer mon compte':'Se connecter'}</button><div className="auth-switch">{mode==='signup'?'Vous avez déjà un compte ?':'Pas encore de compte ?'}<button type="button"onClick={()=>setMode(mode==='signup'?'login':'signup')}>{mode==='signup'?'Se connecter':'Créer un compte'}</button></div></form>}</div></div>}
function ShopForm({session,onClose,onSaved,existing}){const[form,setForm]=useState({name:existing?.shop_name||'',description:existing?.description||'',logo:existing?.logo||'',banner:existing?.banner||'',category:existing?.category||''}),[saving,setSaving]=useState(false),[msg,setMsg]=useState('');const set=(k,v)=>setForm(x=>({...x,[k]:v}));async function save(e){e.preventDefault();if(!form.name.trim())return setMsg('Le nom de la boutique est obligatoire.');setSaving(true);const slug=form.name.toLowerCase().trim().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')+'-'+session.user.id.slice(0,6);const payload={owner_id:session.user.id,shop_name:form.name.trim(),slug,description:form.description.trim(),logo:form.logo.trim(),category:form.category.trim(),status:'pending'};const{data,error}=existing?await supabase.from('shops').update(payload).eq('id',existing.id).select().single():await supabase.from('shops').insert(payload).select().single();if(error)setMsg(error.message);else{await supabase.from('users').update({role:'vendeur',shop_id:data.id}).eq('id',session.user.id);onSaved(data);setMsg('Boutique enregistrée. Elle peut maintenant être partagée.')}setSaving(false)}return <div className="modal-backdrop"onClick={onClose}><div className="cart-modal"style={{maxWidth:760}}onClick={e=>e.stopPropagation()}><div className="modal-head"><div><span className="eyebrow">MA BOUTIQUE</span><h2>{existing?'Modifier ma boutique':'Créer ma boutique'}</h2></div><button onClick={onClose}><X/></button></div>{msg&&<div className="account-message">{msg}</div>}<form onSubmit={save}><div className="form-grid"><label><span>Nom de la boutique *</span><input value={form.name}onChange={e=>set('name',e.target.value)}required placeholder="Ex. PJD Tech Store"/></label><label><span>Catégorie</span><input value={form.category}onChange={e=>set('category',e.target.value)}placeholder="Électronique, Mode..."/></label><label className="field-full"><span>Description de la boutique</span><textarea value={form.description}onChange={e=>set('description',e.target.value)}rows={4}placeholder="Présentez votre boutique, vos produits et vos services."/></label><label><span>Logo (URL)</span><input value={form.logo}onChange={e=>set('logo',e.target.value)}placeholder="https://..."/></label><label><span>Bannière (URL)</span><input value={form.banner}onChange={e=>set('banner',e.target.value)}placeholder="https://..."/></label></div><div className="sub-card"><ImageIcon/><div><b>Identité visuelle</b><p>Le logo et la bannière seront utilisés sur la page publique de votre boutique.</p></div></div><button className="save-btn"disabled={saving}><Store/>{saving?'Enregistrement...':'Enregistrer ma boutique'}</button></form></div></div>}
function ShopView({shop,products,onBack,onAdd}){const url=`${window.location.origin}${window.location.pathname}?boutique=${encodeURIComponent(shop.slug)}`;const[done,setDone]=useState(false);async function share(){try{await navigator.clipboard.writeText(url);setDone(true);setTimeout(()=>setDone(false),2000)}catch{window.prompt('Copiez le lien de votre boutique',url)}}return <main><section className="shop-public"><button className="secondary"onClick={onBack}><ArrowLeft/>Retour au catalogue</button>{shop.logo&&<img className="shop-logo"src={shop.logo}alt=""/>}<div className="shop-banner"style={shop.banner?{backgroundImage:`url(${shop.banner})`}:undefined}></div><div className="shop-head"><div><span className="eyebrow">BOUTIQUE PJD MARKET</span><h1>{shop.shop_name}</h1><p>{shop.description||'Bienvenue dans ma boutique.'}</p></div><button className="primary"onClick={share}>{done?<CheckCircle/>:<Share2/>}{done?'Lien copié':'Partager la boutique'}</button></div></section><section className="section"><div className="section-head"><div><span className="eyebrow">CATALOGUE</span><h2>Produits de {shop.shop_name}</h2></div></div><div className="products">{products.length?products.map(p=><ProductCard key={p.id}p={p}onAdd={onAdd}/>):<div className="empty">Cette boutique ne contient pas encore de produit publié.</div>}</div></section></main>}
export default function MarketplaceShell(){const[menu,setMenu]=useState(false),[search,setSearch]=useState(''),[view,setView]=useState('Accueil'),[products,setProducts]=useState([]),[loading,setLoading]=useState(true),[session,setSession]=useState(null),[authOpen,setAuthOpen]=useState(false),[authMode,setAuthMode]=useState('login'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[code,setCode]=useState(''),[authMessage,setAuthMessage]=useState(''),[cart,setCart]=useState(()=>JSON.parse(localStorage.getItem('pjd-cart')||'[]')),[cartOpen,setCartOpen]=useState(false),[sellerOpen,setSellerOpen]=useState(false),[shop,setShop]=useState(null),[myShop,setMyShop]=useState(null),[authLoading,setAuthLoading]=useState(false),[shopLoading,setShopLoading]=useState(false);useEffect(()=>{supabase.auth.getSession().then(({data})=>setSession(data.session));const{data:l}=supabase.auth.onAuthStateChange((_e,n)=>setSession(n));loadProducts();return()=>l.subscription.unsubscribe()},[]);useEffect(()=>{if(session)loadMyShop(session.user.id)},[session]);useEffect(()=>{localStorage.setItem('pjd-cart',JSON.stringify(cart))},[cart]);useEffect(()=>{const slug=new URLSearchParams(window.location.search).get('boutique');if(slug)openPublicShop(slug)},[]);async function loadProducts(){setLoading(true);const{data}=await supabase.from('digital_products').select('id,seller_id,shop_id,title,description,category,price,promo_price,cover_image,file_type,file_url,is_free,downloads,sales,status,created_at').in('status',['approved','active','actif']).order('created_at',{ascending:false});setProducts(data||[]);setLoading(false)}async function loadMyShop(id){const{data}=await supabase.from('shops').select('*').eq('owner_id',id).maybeSingle();setMyShop(data)}async function openPublicShop(slug){setShopLoading(true);const{data:s}=await supabase.from('shops').select('*').eq('slug',slug).eq('status','approved').maybeSingle();if(s){const{data:p}=await supabase.from('digital_products').select('id,seller_id,shop_id,title,description,category,price,promo_price,cover_image,file_type,file_url,is_free,downloads,sales,status,created_at').eq('shop_id',s.id).in('status',['approved','active','actif']).order('created_at',{ascending:false});setShop(s);setProducts(p||[])}setShopLoading(false)}function openAuth(mode='login'){setAuthMode(mode);setAuthMessage('');setCode('');setAuthOpen(true)}async function signIn(e){e.preventDefault();setAuthLoading(true);const{error}=await supabase.auth.signInWithPassword({email:email.trim().toLowerCase(),password});if(error)setAuthMessage(error.message);else{setAuthOpen(false);setView('Accueil')}setAuthLoading(false)}async function signUp(e){e.preventDefault();setAuthLoading(true);const clean=email.trim().toLowerCase();const{error}=await supabase.auth.signUp({email:clean,password,options:{data:{name:clean.split('@')[0]}}});if(error)setAuthMessage(error.message);else{setAuthMode('verify');setAuthMessage('Compte créé. Consultez votre e-mail pour le code à 6 chiffres.')}setAuthLoading(false)}async function verifyCode(e){e.preventDefault();setAuthLoading(true);const{data,error}=await supabase.auth.verifyOtp({email:email.trim().toLowerCase(),token:code,type:'signup'});if(error)setAuthMessage(error.message);else{setSession(data.session);setAuthOpen(false);setView('Accueil')}setAuthLoading(false)}async function signOut(){await supabase.auth.signOut();setSession(null);setView('Accueil')}async function addToCart(p){const next=cart.some(x=>x.product_id===p.id)?cart.map(x=>x.product_id===p.id?{...x,quantity:x.quantity+1}:x):[...cart,{product_id:p.id,name:p.title,price:Number(p.price),quantity:1}];setCart(next);setCartOpen(true);if(session){let{data:c}=await supabase.from('carts').select('id').eq('user_id',session.user.id).maybeSingle();if(!c)({data:c}=await supabase.from('carts').insert({user_id:session.user.id}).select('id').single());if(c)await supabase.from('cart_items').upsert({cart_id:c.id,product_id:p.id,name:p.title,price:p.price,quantity:next.find(x=>x.product_id===p.id).quantity},{onConflict:'cart_id,product_id'})}}const filtered=products.filter(p=>!search||`${p.title} ${p.category} ${p.description||''}`.toLowerCase().includes(search.toLowerCase()));function createShop(){if(!session){setSellerOpen(false);openAuth('login');return}setSellerOpen('form')}return <div className="marketplace"><div className="topbar"><div>🚚 Livraison · 🔒 Paiement sécurisé · 💬 Support</div><div className="toplinks"><button onClick={()=>openAuth('signup')}>Créer un compte</button><button onClick={createShop}>Créer ma boutique</button></div></div><header className="header"><button className="mobile-menu"onClick={()=>setMenu(!menu)}>{menu?<X/>:<Menu/>}</button><div className="brand"><span className="brand-mark">P</span><span>PJD <span className="brand-dot">MARKET</span></span></div><div className="search"><Search size={20}/><input value={search}onChange={e=>setSearch(e.target.value)}placeholder="Rechercher un produit, une boutique, un service..."/><button onClick={()=>setView('Catalogue')}>Rechercher</button></div><div className="actions"><button className="icon-btn"onClick={()=>setView('Catalogue')}><ShoppingCart/></button><button className="account"onClick={()=>session?signOut():openAuth('login')}>{session?<LogOut size={19}/>:<LogIn size={19}/>}<span>{session?'Déconnexion':'Se connecter'}</span></button>{session&&<button className="account account-signup"onClick={createShop}><Store size={19}/><span>Créer ma boutique</span></button>}</div></header><div className="brand-slogan">Achetez. Vendez. Développez votre activité.</div><nav className={'nav '+(menu?'open':'')}>{nav.map(n=><button key={n}className={view===n?'active':''}onClick={()=>{setView(n);setMenu(false)}}>{n}</button>)}<button className="nav-promo"onClick={()=>setView('Promotions')}>Offres du moment <Zap size={15}/></button></nav>{shop?<ShopView shop={shop}products={products}onBack={()=>{setShop(null);loadProducts();history.replaceState({},'',window.location.pathname)}}onAdd={addToCart}/>:view==='Accueil'?<main><section className="hero"><div className="hero-copy"><span className="eyebrow">PJD MARKET · MARKETPLACE</span><h1>Achetez, vendez et <em>développez votre activité.</em></h1><p>Une marketplace complète pour les produits physiques, produits numériques et services.</p><div className="hero-buttons"><button className="primary"onClick={()=>setView('Catalogue')}>Explorer les catalogues <ChevronRight size={18}/></button><button className="secondary"onClick={createShop}><Store size={18}/>Créer ma boutique</button></div><div className="trust"><span><ShieldCheck/>Paiement sécurisé</span><span><Download/>Produits numériques immédiats</span><span><Truck/>Livraison</span></div></div><div className="hero-panel"><div className="hero-orb">PJD</div><div className="floating-card card-one"><BarChart3/><div><b>{products.length}</b><small>produits disponibles</small></div></div></div></section><section className="section"><div className="section-head"><div><span className="eyebrow">ESPACE VENDEUR</span><h2>Développez votre propre boutique</h2><p>Créez votre vitrine, ajoutez votre logo et votre bannière, puis partagez-la avec vos clients.</p></div></div><div className="categories"><button className="category"onClick={createShop}><span className="cat-icon"><Plus/></span><div><b>Créer sa propre boutique</b><small>Nom, description, logo, bannière et catégorie</small></div><ChevronRight/></button>{myShop&&<button className="category"onClick={()=>setSellerOpen('form')}><span className="cat-icon"><Store/></span><div><b>{myShop.shop_name}</b><small>Modifier ou partager ma boutique</small></div><ChevronRight/></button>}</div></section><section className="section muted"><div className="section-head"><div><span className="eyebrow">CATALOGUE</span><h2>Explorer les catalogues</h2></div><button className="see-all"onClick={()=>setView('Catalogue')}>Voir tout <ChevronRight size={17}/></button></div>{loading?<div className="loading"><Loader2 className="spin"/>Chargement...</div>:<div className="products">{filtered.slice(0,8).map(p=><ProductCard key={p.id}p={p}onAdd={addToCart}/>)}</div>}</section></main>:<main><section className="page-title"><span className="eyebrow">PJD MARKET</span><h1>{view}</h1><p>Explorez les catalogues et trouvez vos produits.</p></section><section className="section"><div className="products">{filtered.map(p=><ProductCard key={p.id}p={p}onAdd={addToCart}/>)}</div></section></main>}{authOpen&&<AuthModal mode={authMode}setMode={setAuthMode}email={email}setEmail={setEmail}password={password}setPassword={setPassword}code={code}setCode={setCode}onSignIn={signIn}onSignUp={signUp}onVerify={verifyCode}onClose={()=>setAuthOpen(false)}loading={authLoading}message={authMessage}/>} {sellerOpen==='form'&&<ShopForm session={session}existing={myShop}onClose={()=>setSellerOpen(false)}onSaved={s=>{setMyShop(s);setSellerOpen(false)}}/>}</div>}
