import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type"};
Deno.serve(async(req)=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});try{const key=Deno.env.get('AI_GATEWAY_API_KEY');if(!key)throw new Error('AI_GATEWAY_API_KEY manquante');const body=await req.json();const products=Array.isArray(body.products)?body.products:[];const stats=body.stats||{};const shop=body.shop||{};const prompt=`Tu es l'assistant commercial de PJD Maker. Analyse les données réelles de cette boutique et donne des recommandations concrètes, prudentes et actionnables. Ne prétends jamais connaître des ventes qui ne sont pas fournies. Boutique: ${JSON.stringify(shop)}. Statistiques catalogue: ${JSON.stringify(stats)}. Produits: ${JSON.stringify(products).slice(0,30000)}. Retourne UNIQUEMENT un JSON valide avec: score (0-100), priority (string), actions (array de 3 à 6 objets {title,reason,action}), price_suggestions (array {product,suggestion}), seven_day_plan (string). Le score doit refléter uniquement les données fournies.`;const r=await fetch('https://ai-gateway.vercel.sh/v1/chat/completions',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json'},body:JSON.stringify({model:'openai/gpt-5.6-luna',messages:[{role:'system',content:'Tu es un consultant e-commerce francophone. JSON strict.'},{role:'user',content:prompt}],temperature:.2})});if(!r.ok)throw new Error(`AI Gateway ${r.status}`);const j=await r.json();const text=j.choices?.[0]?.message?.content||'';const clean=text.replace(/^```json\s*/i,'').replace(/```$/,'').trim();let result;try{result=JSON.parse(clean)}catch{throw new Error('Réponse IA invalide');}return new Response(JSON.stringify({result}),{headers:{...cors,'Content-Type':'application/json'}})}catch(e){return new Response(JSON.stringify({error:e.message||'Erreur IA'}),{status:500,headers:{...cors,'Content-Type':'application/json'}})}});
